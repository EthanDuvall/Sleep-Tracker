import 'package:flutter/material.dart';

class CheckInScreen extends StatelessWidget {

  final double stress;
  final bool sleptWell;
  final String note;


  const CheckInScreen({
    super.key,
    required this.stress,
    required this.sleptWell,
    required this.note,
  });


  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
